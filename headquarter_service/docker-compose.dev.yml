# Use postgres/example user/password credentials
version: '3.1'

services:

  rabbit_mq:
    image: rabbitmq:3-management
    ports:
      - 5672:5672
      - 15672:15672
    volumes:
      - rabbitmq_data:/data

volumes:
  rabbitmq_data: